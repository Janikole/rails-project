<%= render :partial => 'nav' %>


<h3>Checkout</h3>
<table>
  <tr>
    <th>Item:</th>
    <td><%= @product.name %></td>
  </tr>
  <tr>
    <th>Price:</th>
    <td>$<%= @product.price.round(2) %></td>
  </tr>
</table>
