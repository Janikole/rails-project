<%= render :partial => 'nav' %>

<% @products.each do |prod|%>
  <%= render :partial => 'store', :object => prod, :as => 'product' %>
  <%= link_to 'Details', store_path(prod) %>
  <br /><br />
<% end %>

